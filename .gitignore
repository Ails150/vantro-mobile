node_modules/
.expo/
package-lock.json

# One-off patch scripts
fix_*.py
check_*.py

# Expo export output
dist/

# Scratch files
patch_jobs.py


# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Patch helper scripts (one-off Python patches, ps1 scripts, dump files)
# These are transient files used during development sessions and should
# not be committed. Keep them locally for reference.
_*.py
_backups/
patch*.py
patchM*.py
patchH*.py
patch_*.py

.env.local

# Generated by expo prebuild. This is a CNG project: EAS runs prebuild on the
# build machine, and a local one (to verify plugins/withCertPinning.js, say)
# leaves a copy behind that must not be committed -- a stale checked-in android/
# silently overrides every config plugin.
/android/
/ios/
